# Allow SSH access
-A FORWARD -p tcp -m tcp --dport 22 -j ACCEPT

# Enable the Tomcat ports
-A FORWARD -p tcp -m tcp --dport <%= node['abiquo']['tomcat']['http-port'] %> -j ACCEPT
-A FORWARD -p tcp -m tcp --dport <%= node['abiquo']['tomcat']['ajp-port'] %> -j ACCEPT
