# Allow SSH access
-A FORWARD -p tcp -m tcp --dport 22 -j ACCEPT

# Enable the KairosDB port
-A FORWARD -p tcp -m tcp --dport <%= node['abiquo']['kairosdb']['port'] %> -j ACCEPT

# Enable the Cassandra clients port
-A FORWARD -p tcp -m tcp --dport <%= node['cassandra']['config']['rpc_port'].to_i %> -j ACCEPT

# Enable the Cassandra inter-node communication port
-A FORWARD -p tcp -m tcp --dport <%= node['cassandra']['config']['storage_port'] %> -j ACCEPT
